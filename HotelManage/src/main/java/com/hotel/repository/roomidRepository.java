package com.hotel.repository;

import com.hotel.model.roomid;
import org.springframework.data.jpa.repository.JpaRepository;

public interface roomidRepository extends JpaRepository<roomid, Integer>{
}

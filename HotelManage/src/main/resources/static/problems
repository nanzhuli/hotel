问题：
1.  员工的权限，可不可以改成012，不用字符串
    （别的工作时间、职位都是数值，就偏偏这个是字符串，绝了）
2.  employ/add接口报500错误
3.  employ/update接口没作用
4.  错误代码全是0
5.  ismember和isenter可以改成0/1吗


要完成的工作：
-   员工信息-增加（接口）、编辑（接口）、删除（待测）
-   订单-编辑、删除（待测）、查看(异步处理）、结算
-   输入合法性检测